# Copy this file to .env and adjust values as needed.
# Never commit .env — it is git-ignored.

# ── Postgres ──────────────────────────────────────────────────────────────────
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=mlflow_password
POSTGRES_DB=mlflowdb

# ── MinIO ─────────────────────────────────────────────────────────────────────
MINIO_ROOT_USER=minio_user
MINIO_ROOT_PASSWORD=minio_password
MINIO_BUCKET=mlflow-artifacts

# ── Host port mapping ─────────────────────────────────────────────────────────
# Only the HOST ports move, into the 55xx block, to dodge common local ports.
MLFLOW_PORT=5500          # host → container 5000 (MLflow UI)
MINIO_API_PORT=5510       # host → container 9000 (MinIO S3 API)
MINIO_CONSOLE_PORT=5511   # host → container 9001 (MinIO console)
POSTGRES_HOST_PORT=5532   # host → container 5432 (Postgres)

# ── Pipeline (Python client) ──────────────────────────────────────────────────
# No AWS_* credentials here: the client talks only to the tracking server,
# which fetches artifacts from MinIO on its behalf.
MLFLOW_TRACKING_URI=http://127.0.0.1:5500
MLFLOW_EXPERIMENT_NAME=diabetes-week3

PIPELINE_RANDOM_SEED=42
PIPELINE_DATA_PATH=data/diabetes.csv
PIPELINE_TEST_SIZE=0.25
PIPELINE_MAX_ITER=1000

# ── Model registry (new in Week 3) ────────────────────────────────────────────
# The name every model version is registered under.
MLFLOW_REGISTERED_MODEL_NAME=diabetes-classifier

# The alias to move when promoting. "staging" is a name we chose, not a
# built-in MLflow concept.
MLFLOW_MODEL_ALIAS=staging

# Set this to your own name — it is recorded as a tag when you promote.
MLFLOW_MODEL_OWNER=your-name
